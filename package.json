{
  "name": "statrumble-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix statrumble run dev",
    "lint": "pnpm -C statrumble lint",
    "typecheck": "pnpm -C statrumble typecheck",
    "test": "pnpm -C statrumble test",
    "verify": "./scripts/verify.sh"
  }
}
