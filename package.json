{
  "name": "statrumble-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix statrumble run dev",
    "lint": "npm --prefix statrumble run lint",
    "typecheck": "npm --prefix statrumble run typecheck",
    "test": "npm --prefix statrumble run test --if-present",
    "verify": "./scripts/verify.sh"
  }
}
